
mainBoard
revealedBoxes
firstSelection
mouseClicked
mousex
mousey

```text
while 구문안임 
for 로 이벤트 추적 (mouseClicked->True)
mousex,mousey = 마우스 x,y의 위치 
boxx,boxy = 박스위치(mousex,mousey) -> 마우스가 박스 위치에 없으면 None

if 마우스가 박스위에 있음
if 박스가 덮혀있음 and 마우스클릭
박스보이는 애니메이션
revealedBoxes[boxx][boyy]->True,로 상태변화

  if firstSelection == None -> (boxx, boxy)로 상태변화
  else : 박스1의 모양, 컬러 구해서 변수에 각자 집어넣음
           박스2의 모양, 컬러 구해서 변수에 각자 집어넣음
           if 박스1의모양 != 박스2의모양 or 박스1의 색상 != 박스2의 색상 
                   틀린거 볼수 있게 잠깐 멈추기
                   박스 감추는 애니메이션
                   revealedBoxes[박스1] -> False로 상태변화
                   revealedBoxes[박스2] -> False로 상태변화
                     #  위에 if문에서 박스1과 2의 모양과 색상이 틀릴때 
                         처리하는 문장
                         모양과 색상이 같으면 if문은 실행되지 않고, 
                         revealedBoxes[boxx][boyy]->True 한 상태를 그대로 
                         가져 가도됌
                         따로 처리 안해도 됨 
			 elif 모양을 맞추었을때 마지막짝인지 확인(revealedBoxes)
					   게임을 이겼음
					   게임을 리셋시킴
					   게임 데이터 초기화, 게임 시작 
			  firstSelection = None , 박스1과 박스2가 선택되었고 
									  틀렸는지 맞는지 확인되었으니깐
									  다시 처음 박스1를 선택할수있게 
									  상태변화
   
디스플레이 업데이트
FPS(30)으로 맞추어서 while구문 동작 제어

while 구문끝
```