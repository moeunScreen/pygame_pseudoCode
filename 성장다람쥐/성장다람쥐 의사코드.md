```text
while구문시작

    if 무적모드 and 무적기간 > 무적시간:
        무적모드 = False
    
    for 다람쥐 in 다람쥐들:
         다람쥐 움직임
         점프 조절
         
        if 랜덤으로 방향바꾸기(2%확률)
            if 다람쥐['movex'] > 0: #오른쪽을보면 이미지교체
                다람쥐['surface'] = pygame.transform.scale(RIMG,,)
            else: #왼쪽을보면 이미지교체
                다람쥐['surface'] = pygame.transform.scale(LIMG,,)
    
    #적다람쥐,풀제거
    for i in range(len()-1,-1,-1):
        if 활동가능범위밖(camerax,cameray,객체[i])
            del 객체[i]
    
    while len(객체) < 객체유지수
        객체.append(새로운다람쥐(camerax,cameray))
    
    #"camera slack"를 고려한 camerax,cameray셋팅
    플레이어센터x     
    플레이어센터y
    if (화면중앙) - 플레이어센터x > CAMERASLACK
        #플레이어는🔹왼쪽🔹너머
        camerax = 플레이어센터x + CAMERASLACK - 윈도우창반
    elif 플레이어센터x - (화면중앙) > CAMERASLACK 
        #플레이어는🔹오른쪽🔹너머
        camerax = 플레이어센터x - CAMERASLACK - 윈도우창반
    camery도 비슷하게
    
    디스플레이.채우기(풀색상)
    
    for 풀 in 풀들:
        풀상자 = pygame.Rect( (풀객체['x'] - 카메라x,
					          풀객체['y'] - 카메라y,
					          넓이
					          높이 )  )
		디스플레이.bilt(GRASSIMAGES[풀객체['grassImage']], 풀상자)
	
	for 다람쥐 in 다람쥐들:
	    풀이랑 비슷하게 하면됨
	
    #플레이어그리기
    flashIsOn = round(time.time(), 1) * 10 % 2 == 1    
	if not 게임오버 and not (무적모드 and flashIsOn)
	    플레이어['rect'] = pygame.Rect(
	    (플레이어['x'] - 카메라x,
		플레이어['y'] - 카메라y - 점프(플레이어['bounce'],점프간격,높이),
		플레이어['size'],
		플레이어['size']
		)                             )
		디스플레이.blit(플레이어['surface'], 플레이어['rect'])
	
	체력 그리기()
	
	for 이벤트 in pygame.event.get()
	    #키를 입력받음
	    if 키다운
	        if 위
	        elif 아래
	        elif 왼쪽
	            if 플레이어['facing'] != LEFT
	                플레이어['surface'] = 이미지L로바꿔주기
	         elif 오른쪽
	            if 플레이어['facing'] != RIGHT
	                플레이어['surface'] = 이미지R로바꿔주기
	        elif 이김 and r키누름
	            return
	    
	    elif 키업(키보드에서손뗌)
	        if 위
	            위 = False
	        elif 아래
	        elif 왼쪽
	        elif 오른쪽
	        elif 끝내기키
	
	# 입력받은 키로 플레이어를 진짜로 움직이게 상태를 변화시킴 
	if not 게임오버
	    if 왼쪽
	        플레이어['x'] -= MOVERATE
	    if 오른쪽
	        플레이어['x'] += MOVERATE
	    if 위
	        플레이어['y'] -= MOVERATE
	    if 아래
	        플레이어['y'] += MOVERATE
	    
	    if (왼 or 오 or 위 or 아래) or 플레이어['bounce'] != 0
	        플레이어['bounce'] += 0
	    if 플레이어['bounce'] > BOUNCERATE
	        플레이어['bounce'] = 0
	          
	# 적다람쥐랑 충돌검사                      
	for i in range(len(다람쥐들)-1,-1,-1)
	    다람쥐 = 다람쥐들[i]
	    if 'rect' in 다람쥐 
	        and 플레이어['rect'].colliderect(다람쥐['rect'])
	        if 다람쥐크기 < 플레이어크기 
	            플레이어크기키우기 
	            del 다람쥐[i]
	        
		        if 플레이어['facing'] == LEFT
		            플레이어['surface'] = 
		                  pygame.transform.scale(limg,(size,size))
		        오른쪽일때도 rimg로 똑같이 한번더
		        
		        if 플레이어['size'] > 이김사이지
		            이김모드 = True
		            
		    elif not 무적모드
		        무적모드 = True
		        무적시작시간 = time.time()
		        플레이어['health'] -= 1
		        if 플레이어['health'] == 0
		            게임오버 = True
		            게임오버시작시간 = time.time()
		
		else
		    디스플레이.bilt(게임오버Surf,게임오버Rect)
		    if 일정시간후
		        return # 현재게임 끝내기
		
		if 이김모드
		    디스플레이.blit(이김Surf,이김Rect)
		
		pygame.display.update()
		FPSCLOCK.tick(30)

while구문끝   
```