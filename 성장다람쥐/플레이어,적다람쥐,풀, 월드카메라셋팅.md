
## 플레이어
```text
playerObj = {'surface': pygame.transform.scale(L_SQUIR_IMG, (STARTSIZE, STARTSIZE)),  
             'facing': LEFT or RIGHT,  
             'size': 25~300,  
             'x': world x pixel,  
             'y': world y pixel,
             'bounce':0~6,  
             'health': 3~0
             'rect' : pygame.Rect object
            }
```
xy좌표는 픽셀의 맨 왼쪽위를 말함

***적다람쥐와 풀객체는 모두 리스트안에 저장***
`squirrelObjs = []` 안에 적다람쥐 객체(딕셔너리형태)를 하나씩 넣어줌
## 적다람쥐객체
```text
squirrelObj = {'surface' : img,
               'x': world x pixel,  
               'y': world y pixel, 
               'movex' : +-pixel,
               'movey' : +-pixel,
               'width' : image pixel,
               'height' : image pixel,
               'bounce' : 0 ~ BOUNCERATE(int),
               'bouncerate' : divide the frame by the bounceRate(int),
               'bounceheight' : how high (in pixels)
               'rect' : pygame.Rect object
               }
```
xy좌표는 픽셀의 맨 왼쪽위를 말함

`grassObjs = []` 안에 풀객체(딕셔너리형태)를 하나씩 넣어줌
## 풀객체
```text
전역 변수로 

GRASSIMAGES = []  
for i in range(1, 5):  
    GRASSIMAGES.append(pygame.image.load('grass%s.png' % i))
를 저장해놓고    
 
grassObj = {'grassImage' : random.randint(0, len(GRASSIMAGES) - 1)
            'x': world x pixel,  
            'y': world y pixel,
            'width' : image pixel,
            'height' : image pixel,
            'rect' : pygame.Rect object
            }   
```
xy좌표는 픽셀의 맨 왼쪽위를 말함

## 메인함수에서 카메라x,카메라y 셋팅
```text
camerax = 0  
cameray = 0
```

카메라x,카메라y 상태업데이트
(카메라 여유거리같이 포함 as "camera slack")
```text
# "camera slack" 포함해서 카메라상태 업데이트
플레이어센터x = 플레이어['x'] + int(플레이어['size'] / 2)  
플레이어센터y = 플레이어['y'] + int(플레이어['size'] / 2)  
if (카메라x + 윈도우창반) - 플레이어센터x > CAMERASLACK:  
    카메라x = 플레이어센터x + CAMERASLACK - 윈도우창반
elif 플레이어센터x - (카메라x + 윈도우창반) > CAMERASLACK:  
    카메라x = 플레이어센터x - CAMERASLACK - 윈도우창반  
    #카메라y계산법은 카메라x와 동일
if (cameray + HALF_WINHEIGHT) - playerCentery > CAMERASLACK:  
    cameray = playerCentery + CAMERASLACK - HALF_WINHEIGHT  
elif playerCentery - (cameray + HALF_WINHEIGHT) > CAMERASLACK:  
    cameray = playerCentery - CAMERASLACK - HALF_WINHEIGHT
```

```text
if (화면중심) - 플레이어센터x > CAMERASLACK:
    # slack포함한 화면중심이 플레이어 센터보다 더 오른쪽에 있음
        = 플레이어센터x는 slack넘어서 🔹왼쪽🔹에 있음 
    카메라x = 플레이어센터에 CAMERASLACK더하고 윈도우창반만큼 뺌
elif 플레이어센터x - (화면중심) > CAMERASLACK:
    # 플레이어센터x는 (화면중심 + slack)넘어서 🔹오른쪽🔹에 있음 
    카메라X = 플레이어센터에 CAMERASLACK빼고 윈도우창반큼 뺌
```

