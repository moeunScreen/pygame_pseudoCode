
### 상태
`pattern = [ ]`
`currentStep =`

```text
while 루프
clickedButton = None
백그라운드,버튼,글자 쓰기

모드 / 상태
모드1 - 입력 기다리기(False)
          디스플레이.업데이트
          시간.기다리기
        패턴.잇기((YELLOW, BLUE, RED, GREEN))
          for 버튼 in 패턴:
                 버튼 깜빡임
                 시간.기다리기(깜빡임 딜레이)
           모드 =  입력하기(True)
모드2 - 
            if 버튼클릭 and 버튼클릭 == 패턴[맞는버튼 인덱스]
                   버튼 깜빡임(누른버튼)
                   맞는버튼 인덱스 += 1
                   저번 누른 시간 = 시간.시간()
                   if 맞는버튼 인덱스 == len(패턴)
                          백그라운드 바꾸는 애니메이션
                          score += 1
                          입력 기다리기 = False
                          맞는버튼 인덱스 = 0
           elif 버튼클릭 and 잘못된 패턴 or (처음버튼x and 시간오버):
                 # 시간 오버 
                     time.time()은 1970년 1월 1일 이후 초단위를 불러옴 
                     time.time() - TIMEOUT(4초) > lastClickTime        
                   게임오버()
                   패턴 = []
                   입력 기다리기 = False  
                   score = 0
                   time.wait(1000)
               백그라운드 바꾸는 애니메이션

디스플레이.업데이트()
FPSCLOCK.TICK(30)

```